# Copy this file to `frontend/.env` for local development.
# VITE_API_URL is the base URL of the backend API used by the Vite dev server.
VITE_API_URL=http://localhost:8000
# MAILCATCHER_HOST is only used by Playwright tests to read captured emails.
MAILCATCHER_HOST=http://localhost:1080
