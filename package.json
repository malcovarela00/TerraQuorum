{
  "name": "terraquorum",
  "private": true,
  "version": "0.1.0",
  "description": "Open Source platform for country analysis, indicator comparison, and AI-assisted diplomatic simulations.",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "bun run --filter frontend dev",
    "lint": "bun run --filter frontend lint",
    "test": "bun run --filter frontend test",
    "test:ui": "bun run --filter frontend test:ui"
  }
}
